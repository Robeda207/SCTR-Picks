# SCTR Scraper App

This app shows the current **top 10 SCTR stocks** by scraping the public StockCharts SCTR page with Playwright.

## Why Playwright?
The StockCharts SCTR page requires JavaScript to populate the rankings table, so a simple HTML request is often not enough.

## Setup

```bash
npm install
npm run dev
