# SCTR Render Deploy App

This app shows the current top 10 SCTR stocks scraped from the public StockCharts SCTR page.

## Local run

```bash
npm install
npx playwright install --with-deps chromium
npm start
