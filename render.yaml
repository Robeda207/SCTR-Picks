services:
  - type: web
    name: sctr-render-app
    runtime: node
    plan: free
    buildCommand: npm install && npx playwright install --with-deps chromium
    startCommand: npm start
    autoDeploy: true
